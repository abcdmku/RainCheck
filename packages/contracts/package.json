{
  "name": "@raincheck/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "format": "biome format --write src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@tanstack/ai": "^0.8.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
